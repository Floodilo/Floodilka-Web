bind 0.0.0.0
port 6379
protected-mode no

requirepass ${LIVEKIT_VALKEY_PASSWORD}
masterauth ${LIVEKIT_VALKEY_PASSWORD}

${VALKEY_REPLICAOF_DIRECTIVE}

maxmemory 1gb
maxmemory-policy noeviction

appendonly no
save ""

loglevel notice
logfile ""
